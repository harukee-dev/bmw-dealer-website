import React from "react";
import classes from "./FeaturesPage.module.css"

export const FeaturesPage = () => {
    return (
        <div className={classes.featuresPage}>
            <h1 className={classes.testText}>Features Page</h1>
        </div>
    )
}