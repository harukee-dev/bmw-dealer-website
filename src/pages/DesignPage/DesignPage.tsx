import React from "react";
import classes from "./DesignPage.module.css"

export const DesignPage = () => {
    return (
        <div className={classes.designPage}>
            <h1 className={classes.testText}>Design Page test</h1>
        </div>
    )
}