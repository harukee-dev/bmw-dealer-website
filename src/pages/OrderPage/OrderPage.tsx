import React from "react";
import classes from "./OrderPage.module.css"

export const OrderPage = () => {
    return (
        <div className={classes.orderPage}>
            <h1 className={classes.testText}>Order Page</h1>
        </div>
    )
}